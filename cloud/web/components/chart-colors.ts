import type { Color } from "@tremor/react";

// tailwind.config.ts remaps these palette-500s to the brand mono ramp;
// keep the hex list (legend dots) in sync with it.
export const MONO_COLORS: Color[] = ["pink", "fuchsia", "rose", "violet"];
export const MONO_HEX = ["#FF1F8E", "#18181B", "#A1A1AA", "#D4D4D8"];
